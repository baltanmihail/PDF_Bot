# PDF Bot - Telegram бот для объединения Word-отчетов

Telegram-бот, который автоматически объединяет Word-файлы из ZIP-архива в один PDF-файл, сортируя их по номерам страниц.

## Возможности

- 📦 Принимает ZIP-архив с Word-файлами
- 🔍 Автоматически находит все Word-файлы (.doc, .docx) в архиве
- 📄 Сортирует файлы по номерам страниц из названий
- 🔄 Конвертирует Word-файлы в PDF
- 📎 Объединяет все PDF в один файл
- 📊 Выводит статистику по обработанным файлам

## Требования

### Системные требования

- **Windows** (для конвертации Word в PDF требуется Microsoft Word)
- Python 3.8+
- Microsoft Word установлен на системе (для конвертации)

### Python-зависимости

Все зависимости указаны в `requirements.txt`. Установите их командой:

```bash
pip install -r requirements.txt
```

## Настройка

1. Получите токен бота от [@BotFather](https://t.me/BotFather) в Telegram
2. Сохраните токен в файле `config.py`:
   ```python
   TOKEN = "ваш_токен_бота"
   ```

## Запуск

```bash
python main.py
```

## Использование

1. Запустите бота командой `/start`
2. Упакуйте все Word-файлы вашего отчета в ZIP-архив
3. Отправьте ZIP-файл боту
4. Дождитесь обработки
5. Получите готовый объединенный PDF-файл

## Правила именования файлов

Бот определяет порядок файлов по номерам страниц в названиях:

- **Титульный лист**: должен содержать слово "титул" в названии (будет первой страницей)
- **Номера страниц**: используйте форматы:
  - `стр. 2-5` или `стр 2-5`
  - `стр. 6` или `стр 6`
  - `стр. 12-16`

### Примеры правильных названий:
- `титул 460.doc`
- `стр 2-5  Список исполнит..doc`
- `стр 6. Реферат.doc`
- `стр. 12-16 ВВЕДЕНИЕ.doc`

### Примечания:
- Файлы с одинаковыми именами будут пропущены (останется только первый)
- Если номер страницы не найден, файл будет добавлен в конец

## Альтернативы Telegram (без отправки файлов в бота)

Если Telegram в регионе тормозит или неудобно загружать большие файлы, можно объединять отчёты локально или через свой сайт.

### 1. Локальный веб-интерфейс (рекомендуется)

Откройте в браузере страницу на своём компьютере, загрузите ZIP и скачайте PDF. Конвертация идёт через установленный Word (Windows).

```bash
pip install fastapi uvicorn python-multipart
uvicorn web_app:app --host 127.0.0.1 --port 8000
```

Откройте в браузере: **http://127.0.0.1:8000**

### 2. Локальный скрипт (ZIP или папка → PDF)

Без браузера: один архив или папка с .doc/.docx → один PDF.

```bash
python run_local_merge.py "путь/к/архиву.zip"
# или папка с Word-файлами:
python run_local_merge.py "путь/к/папке"
```

PDF сохраняется рядом с архивом (имя из названия ZIP) или в папке как `merged.pdf`.

### 3. Деплой на Railway (или другой хостинг)

На сервере нет Word, поэтому конвертация идёт через **LibreOffice**. Нужен образ/среда с установленным LibreOffice.

- **Procfile** (уже в проекте): `web: uvicorn web_app:app --host 0.0.0.0 --port $PORT`
- В **Railway**: создать сервис из репозитория, в настройках добавить **Dockerfile** или использовать стек с LibreOffice (например, Docker-образ `linuxserver/libreoffice` или кастомный Dockerfile с `apt-get install libreoffice`).
- Переменные не обязательны; при необходимости путь к LibreOffice задаётся в коде (см. `core.py`, `_convert_word_libre`).

Кросс-платформа: локально на Windows — Word; на Linux/сервере — LibreOffice.

## Структура проекта

```
PDF_Bot/
├── config.py            # Конфигурация (токен бота)
├── main.py              # Telegram-бот
├── core.py              # Общая логика: сортировка, конвертация, объединение PDF
├── web_app.py           # Веб-интерфейс (FastAPI)
├── run_local_merge.py   # CLI: ZIP/папка → PDF
├── requirements.txt
├── Procfile             # Для Railway
└── README.md
```

## Обработка ошибок

Бот обрабатывает следующие ситуации:
- Отсутствие Word-файлов в архиве
- Ошибки конвертации отдельных файлов
- Ошибки при объединении PDF
- Неправильный формат файла (не ZIP)

## Логирование

Все операции логируются в консоль. Уровень логирования: `INFO`.

## Безопасность

⚠️ **Важно**: Файл `config.py` содержит секретный токен бота. Не добавляйте его в систему контроля версий (Git).

Создайте файл `.gitignore` и добавьте туда:
```
config.py
```

